<p>
follower:<br>
<% Relation.where(:to_user_id => @user.id).each do |relation| %>
	<% follower = User.find(relation.user_id) %>
	<%= image_tag follower.icon %>
	<%= follower.id %>
	<%= link_to follower.name, user_path(follower.name) %>
<% end %>
</p>