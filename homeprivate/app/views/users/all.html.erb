<h1>All Users</h1>
<ul>
	<% @all_users.each do |user| %>
		<li>
			<%= image_tag(user.icon, :size => "100x100") %>
			<%= user.name %>,
			<%= link_to user.username, user.username %>
			<%= user.detail %>,
		</li>
	<% end %>
</ul>